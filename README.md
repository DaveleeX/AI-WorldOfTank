# IRON FRONT · 废土战线

可游玩的第三人称 3D 坦克歼灭战。运行 `npm run dev` 后访问 http://localhost:3000 。建议使用支持 WebGL2 的桌面浏览器。

## 游玩

选择中国 99A、美国 AbramsX、英国挑战者 2、法国勒克莱尔、德国豹 2A7、日本 10 式，在沙漠、城市、泥泞乡村和雪地展开 5v5 对战。玩家与 4 辆 AI 队友对阵 5 辆敌车；玩家阵亡后继续观战，直到一方全灭。

- W / S：前进 / 倒车；A / D：车身转向。
- 鼠标：左右转动炮塔，上下控制炮管俯仰（−10°～+25°）；点击战场锁定鼠标。远距离射击需要抬高炮口补偿重力下坠。
- 左键：发射，按住可连续装填发射；右键：按住放大瞄准。
- Shift：消耗能量加速，松开恢复；P / Esc：暂停。
- 胜利奖励 500 积分，每辆玩家击毁的敌车额外 100 积分；失败不奖励。
- 炮管、履带、弹药、装甲可永久采购或换装，影响实际伤害、装填、机动、能量、溅射和生命值。
- 积分和装备保存在当前设备浏览器中，初始积分为 0。

## 模型与场景

六款坦克全部使用 Blender 模型。英国、德国、美国复用工程既有资产；中国、法国、日本通过运行中的 Blender MCP，依据实车参考图与原工程的细节建模函数制作。新增模型每款约 1,100 个零件，使用与原车型相同的游戏减面比例。保留独立炮塔，所有车辆不烘焙光照。

四个静态战场通过 Blender MCP 创建，与游戏碰撞数据共用同一份布局。每个环境用 Cycles 烘焙 2048×2048 的纯间接漫反射贴图（16 samples），不包含直接光和材质颜色；游戏中通过线性 lightMap 加载，实时太阳提供直射和阴影。动态车体接受实时天空光；静态材质在着色器中禁用实时天空漫反射，避免与烘焙光重复。坦克、炮弹、爆炸均不在静态烘焙场景中。

- `assets/blender/*.blend`：三款新车；`assets/worlds/*.blend`：四个静态世界的可编辑源文件。
- `assets/REFERENCES.md`：图片来源及外形重建记录。
- `assets/blender/tank-report.json`、`assets/worlds/bake-report.json`：建模与实际烘焙记录。
- `scripts/build_missing_tanks.py`、`build_worlds_final.py`：Blender MCP 建模脚本。
- `lib/battle.mjs`：战斗、AI、装备和结算逻辑。
- `lib/renderer.ts`：Three.js 模型、光照、相机与特效。

## 验证与范围

`node scripts/test-battle.mjs` 验证 5v5、装备、移动能量、遮挡和命中、胜负与四地图 AI 战斗可结束；`node scripts/test-models.mjs` 验证六款实际 GLB 的独立炮塔；`node scripts/test-assets.mjs` 验证单场景导出、精度、UV 和烘焙图。`npx tsc --noEmit` 与 `npm run build` 检查类型和发布构建。

这是单人 AI 对战游戏，不包含联网多人。地图为 360 米开放战场，炮弹使用 9.81 m/s² 重力、135～180 m/s 的游戏初速和三维包围盒命中检测；未模拟空气阻力、风偏或真实装甲穿透。美术保留已有坦克的风格化比例，不是实车精密工程复刻。烘焙采用实时游戏预算，不声称达到商业大作质量。本轮未进行浏览器画面和鼠标操作实测；Blender 资产另有渲染检查。进度不跨设备同步。

## 本次复核

通过独立 Blender MCP 会话重新打开三款新车和四张地图源文件，确认三车分别含 1166、1146、1119 个网格，四图均为 Cycles 纯间接漫反射烘焙，动态对象未进入烘焙。记录见 `assets/source-audit.json`。修复重复出击、重复释放预览模型、切图时烘焙贴图释放和暂停键长按反复切换。新增共享渲染资源释放回归检查。初次复核时线上 Sites 服务返回 HTTP 500。本次更新时服务已恢复，但本机域名策略对 git.chatgpt-team.site 返回 HTTP 403，源码上传被拦截，线上版本未更新。当前新增功能可通过本地入口体验。

## 过场、军械库与战斗演出

- 打开游戏和部署战斗时播放 7.5 秒护航飞行器过场，镜头最终落到当前选中的坦克；可以跳过。部署过场期间 AI 与计时暂停。
- 默认仅显示导航与快捷出击。点击车库或军械库才展开对应面板，再次点击或使用关闭按钮收起。
- 军械库选择炮管、履带、装甲或弹药时平滑切换特写；“预览外观”不消费积分、不修改已装备状态。
- 两种炮管、两种履带、两种装甲、三种弹药和飞行器均由 Blender MCP 实际生成。源库位于 `assets/blender/equipment-library.blend`，游戏文件位于 `public/equipment/`。
- 六车重新导出为独立车身、炮塔、主炮和履带；文件在 `public/models/rigged/`。导出脚本从实际炮口位置计算原始仰角，归零后绑定俯仰轴，画面炮口与物理发射点一致。
- 个人本局击毁数依次触发单杀、双杀、三杀、四杀、五杀五级演出。仅计玩家击毁，不计队友击毁；五杀提示可覆盖结算画面，结算不会截断奖励音。
- `lib/music.mjs` 是原创实时合成配乐：沙漠《尘暴追猎》（126 BPM）、城市《钢铁封锁》（140 BPM）、乡村《边境暗涌》（112 BPM）、雪地《白色警戒》（118 BPM）。首次点击后启用声音；可静音，暂停停止战斗配乐。没有调用外部音频服务或使用商业录音。
- `node scripts/test-cinematic-combat.mjs` 检查重力轨迹、俯仰限位、掩体高度、炮口穿墙、远距离射击、五级击毁、18 种车型/装备组合、预览回退、过场跳过和音乐调度生命周期。音频调度使用模拟音频设备进行测试，未声称已在浏览器中试听。
- `assets/equipment-review.png` 为从实际游戏 GLB 重导入 Blender 后生成的外观检查图。
